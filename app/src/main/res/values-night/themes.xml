<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Newsify" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Newsify" parent="Base.Theme.Newsify" />

    <style name="CustomSearchViewTheme" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">#605757</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorControlActivated">#574B4B</item>
        <item name="android:editTextColor">@color/black</item>
    </style>
</resources>